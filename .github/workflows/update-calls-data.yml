name: Update EU calls data

on:
  workflow_dispatch:

jobs:
  no-file-snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Snapshot disabled
        run: |
          echo "This project now loads EU calls through /api/calls with a 30-minute Vercel memory cache."
          echo "No JSON snapshot commit is required."
